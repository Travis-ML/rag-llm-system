# Ollama Logging Configuration
# Set to 'true' to enable comprehensive logging to Splunk HEC
# IMPORTANT: Currently REQUIRED for system to function properly
# TODO: Make logging optional without breaking Open WebUI functionality
ENABLE_LOGGING=true

# Splunk HEC Configuration (only needed if ENABLE_LOGGING=true)
# Using Docker Splunk container on same network (HTTPS required)
SPLUNK_HEC_URL=https://splunk:8088/services/collector/event

# Splunk HEC Token - Generate your own unique UUID:
# Windows: powershell -Command "[guid]::NewGuid().ToString()"
# Linux/Mac: uuidgen | tr '[:upper:]' '[:lower:]'
# Or use: https://www.uuidgenerator.net/
# Default example token (replace with your own for production):
SPLUNK_HEC_TOKEN=00000000-0000-0000-0000-000000000000

VERIFY_SSL=false

# Note: Copy this file to .env and modify as needed
# The .env file is excluded from git for security
